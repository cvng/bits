# https://doc.rust-lang.org/cargo/reference/config.html

[alias]
dev = "watch --clear --exec run"
lint = "clippy"
next = "nextest run"
snap = "insta review"
task = "run --bin task --quiet --release --"

[build]
target-dir = ".build"
